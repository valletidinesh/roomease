package com.roomease.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette ─────────────────────────────────────────────────────────────
val Primary      = Color(0xFF6366F1)   // Indigo-500
val PrimaryDark  = Color(0xFF4F46E5)   // Indigo-600
val Secondary    = Color(0xFF10B981)   // Emerald-500
val Tertiary     = Color(0xFFF59E0B)   // Amber-500

// ── Surfaces (dark theme) ─────────────────────────────────────────────────────
val Background   = Color(0xFF0F0F13)
val Surface      = Color(0xFF18181F)
val SurfaceVar   = Color(0xFF22222C)
val Outline      = Color(0xFF2E2E3A)

// ── Text ──────────────────────────────────────────────────────────────────────
val OnBackground = Color(0xFFF1F1F5)
val OnSurface    = Color(0xFFE4E4EB)
val OnSurfaceVar = Color(0xFF8B8B9E)

// ── Task accent colors ────────────────────────────────────────────────────────
val CookingColor   = Color(0xFFE8FF5A)   // Yellow-green
val TrashColor     = Color(0xFFFF6B35)   // Orange
val WashroomColor  = Color(0xFF5ADFFF)   // Cyan
val WaterColor     = Color(0xFF5AFFA0)   // Mint
val ConsumeColor   = Color(0xFFC77DFF)   // Purple
val BuyListColor   = Color(0xFFFFB347)   // Amber

// ── Semantic ──────────────────────────────────────────────────────────────────
val Success  = Color(0xFF22C55E)
val Warning  = Color(0xFFF59E0B)
val Error    = Color(0xFFEF4444)
